# SPEC t6  (class Scene_t6, file scenes/t6.py)

- Exam tag: **JEE ADVANCED 2023 · PAPER 1** · format **SINGLE CORRECT**
- Chapter: Thermodynamics

## Question
One mole expands ADIABATICALLY from (T_A, V₀) to (T_f, 5V₀). Another mole of the same gas expands
ISOTHERMALLY from (T_B, V₀) to the same final state (T_f, 5V₀). With γ = C_P/C_V, find T_A/T_B.
- (A) 5^(γ−1)   (B) 5^(1−γ)   (C) 5^γ   (D) 5^(1+γ)

## KNOWN ANSWER (anchor to this): (A) 5^(γ−1)

## Concept-first
- Isothermal process: temperature constant throughout ⇒ its endpoints share one T.
- Adiabatic process: T V^(γ−1) = const.

## LIVE VISUAL
Draw one P–V plane with BOTH curves starting at V₀ and ending at 5V₀: an isotherm (EMBER, gentler)
and an adiabatic (IGNITION, steeper). Mark start/end. Optionally a live V readout V₀ → 5V₀ as a dot
travels. Dock bottom-right after.

## STEP RAIL (LAW → SUBSTITUTION → RESULT)
1. Isothermal branch: T is constant and ends at T_f → **T_B = T_f**.
2. Adiabatic branch `T V^(γ−1) = const`: `T_A V₀^(γ−1) = T_f (5V₀)^(γ−1)`
   → **T_A = T_f · 5^(γ−1)**.
3. Ratio `T_A/T_B = (T_f · 5^(γ−1)) / T_f` → **T_A/T_B = 5^(γ−1)** → option **(A)**.
   Mark (A) CORRECT green, others ERROR red. (results rail: `T_A/T_B = 5^{\gamma-1}`).

Answer lock-in: **(A) 5^(γ−1)**.
